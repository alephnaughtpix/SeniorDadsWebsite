%p#notice= notice

%p
  %b Name:
  = @category.name
%p
  %b Title:
  = @category.title
%p
  %b Parent category:
  - unless @category.parent_category_id.nil?
    = Category.where( id: @category.parent_category_id ).first.title

= link_to 'Edit', edit_category_path(@category)
\|
= link_to 'Back', categories_path
