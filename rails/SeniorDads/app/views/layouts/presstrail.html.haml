- content_for :title do
  dA pR3sS tRa1L!!11!: #{yield :sub_title}
- content_for :keywords do
  publicity, press, print, online, magazines, #{yield :sub_keywords}
- content_for :style_id, flush: true do
  = content_for?(:style_id) ? yield(:style_id) : "presstrail"
- content_for :main do
  = render "partials/last_updated"
  = render "partials/sub_header_sub_feature", :is_root => @is_root, :section_title => "pR3sS tRa1L!!11!"
  = content_for?(:content) ? yield(:content) : yield
= render :file => 'layouts/application'